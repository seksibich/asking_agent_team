# 关注与持仓（服务端当前状态镜像）

> 唯一事实源：数据服务 `portfolio_get`。本文件只保存连接、同步版本和响应 rows 的可读镜像。

- BASE_URL：`http://localhost:18901`
- portfolio_version：
- 最近同步时间（Asia/Shanghai）：
- 数据来源：`portfolio_get`

## 持仓（type=holding）

| 代码 | 名称 | 持仓成本 | 手数 | 对应股数 | 备注 | 服务端更新时间 |
|---|---|---:|---:|---:|---|---|

## 关注（type=watch）

| 代码 | 名称 | 备注 | 服务端更新时间 |
|---|---|---|---|

> 同步流程：读取本文件 BASE_URL → 调 `/health` 比较 `portfolio_version` → 不一致时调用 `portfolio_get` → 用同一响应的 rows 和版本全量覆盖。
> 禁止在此记录关联板块、分析结论、工作进度、故障、待办或待同步事项；同步失败写入 `短期记忆/`，成功后删除。